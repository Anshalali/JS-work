// Comparison equality operator it can be used to compare values
// (comparison equality operator)

function testEqual(val) {
    if (val == "anshal") {
        return "Equal";
    }
    return "Not Equal";
}

console.log(testEqual("hello"))