// conditional logic if statement it can be used to make decisions
// use conditional logic with if statement

function trueOrFalse(isItTrue) {
    if (isItTrue) {
        return "yes, it's true";
    }
    return "no, it's false";
}

console.log(trueOrFalse(true))